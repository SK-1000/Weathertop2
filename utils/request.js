//api c09a714e06b0144a9c24d78ca2a12cd1


/*
function getWeatherReport(latitude, longitude) {
  const axios = require("axios");

  const url =
    "http://api.openweathermap.org/data/2.5/weather?lat=${latitude}&lon=${longitude}&appid=c09a714e06b0144a9c24d78ca2a12cd1&units=metric";

  //Make a request for a user with a given ID
  axios
    .get(url)
    .then(function(response) {
      // handle success
      document.getElementById("message").innerText =
        "It is now ${response.data.main.temp} C";
      //console.log(response.data.main.temp);
    })
    .catch(function(error) {
      // handle error
      console.log(error);
    })
    .finally(function() {
      console.log("All done");
      // always executed
    });
}

*/